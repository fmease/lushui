;;; TEST ignore
;;; small program which I used to start developing the code generator – what I should continue on doing
;;; – after finally implementing the bidirectional type checker with implicit arguments, sigh!

@foreign
data Nat32: Type

second (a b: Nat32): Nat32 = b

identity (a: Nat32): Nat32 = a

identity2 (x: Nat32): Nat32 = second x x

main: Nat32 = identity (identity (second (second @Nat32 6666 @Nat32 23) (second @Nat32 1 (identity2 @Nat32 100))))
