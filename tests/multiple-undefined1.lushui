;; TEST ignore

use alpha.Nothing ;; @Bug this is a fatal error, it shouldn't be

module alpha: =
    Thin: Type = super.Air
    use super.Air
    use crate.empty as full

    module beta: =
        use super.air

use alpha.(Nothingness beta.space)

