;; TEST ignore

;; use alpha.alias

;; module alpha: =
;;     use beta.alias

;;     module beta: =
;;         alias: Type = Type


;; @Bug loops forever: we need to check what we already checked
;; module mu: =
;;     use super.nu.A

;; module nu: =
;;     use super.mu.A

;; X: Type = Alpha

;; data Alpha: Type =
;;     alpha: Alpha

;; module foo: =
;;     use super.Alpha
;;     use super.alpha

;;     what-every: Alpha = alpha

;;     use super.Void

;;     V: Type = Void

;; data Void: Type =

;; @Note very difficult test case:
use alpha.Void

module alpha: =
    use super.V

    module xoo: =
        W: Type = super.V

    data Void: Type =

V: Type = Void

;; Self: Type = Self
