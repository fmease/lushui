;; TEST pass check

module core

use core.list.List
use core.list.nil
use core.list.cons
use core.bool.Bool
use core.bool.false
use core.bool.true
use core.unit.Unit
use core.unit.unit

data Any: Type =
    any: (A: Type) -> A -> Any

foo: List Bool =
    cons Bool false (cons Bool true (nil Bool))

bar: List Any =
    cons Any (any Unit unit) (cons Any (any Bool false) (nil Any))
