;;; Messing with indentation

identity (A: Type): A -> A = \(a: A): A => a

number: Nat =
    identity Nat 45
next: Nat = 200

;;; @Bug apparently unexpected dedentation
foo (x: Nat): Nat =
    case x
    of 0 of 1 => 10
    of x => x
