We have to create one package per component with faulty path instead of a single one
containing several components since the compiler currently fails the compilation of a
package once it fails on a single component.
