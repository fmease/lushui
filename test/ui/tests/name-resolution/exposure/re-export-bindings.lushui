;;; TEST pass check

use Identity as Alias
data Identity: Type of

@public use Point as *
@public data Point: Type of

use capsule as krate ;;; @Bug needs to be above `Center`

@(public capsule) use Center as C
@(public krate) data Center: Type of

module namespace of
    T: Type = Type

    module subnamespace of
        @(public super)
        use super.T as T0

        @(public capsule.namespace.subnamespace)
        use super.T as T1
