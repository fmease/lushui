;;; TEST pass check

module inner of
    use innermore.innermost as m

    @(public super.krate)
    use capsule.offside as remote

    @(public capsule)
    module innermore of
        @(public super)
        module innermost of
            @(public capsule.inner)
            X: Type = X

            @(public capsule.n)
            Th: Type = Type

        TH: Type = innermost.Th ;;; @Beacon @Bug lexing/parsing: this is placed in the same line as `innermore`, `Z`!

    Z: Type = m.X

use inner.innermore as n
use self as krate

module offside of
    @(public capsule.inner.remote) data Ping: Type of
