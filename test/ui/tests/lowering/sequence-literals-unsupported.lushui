;;; TEST fail check

use extern.core.(list.List nat32.Nat32 text.Text)

construct: List = List.[1 3 100 Nat32.0]


destruct: X =
    case source of
        [\x \y Text."X"] => always
        \__ => forever
