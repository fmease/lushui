# @Task create several internal testing attributes with various characteristics and extend this test with them

@(allow never-gonna-be-implied)
module m of

@(allow "my lint")
@(forbid (lint "my other one"))
@deny
data Void: Type of

@(public (reech path) "hello")
base: Base = @(static 0) "X"
