# @Task create several internal testing attributes with various differences
# and extend this test file here

@intrinsic
@intrinsic
use self.()

@intrinsic @abstract @(location "/")
use something.substantial.(root0, root1)

@include
binding: Binding =
    @intrinsic make-binding @(public super) resource

data Manifest of
    @public manifest

Platform =
    case x of
        @static box => it
        let _ => unboxed
