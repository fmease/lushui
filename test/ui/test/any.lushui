;;; TEST pass check

use extern.core.(
    type.Type
    list.(List empty prepend)
    bool.(Bool false true)
    unit.(Unit unit)
    )

data Any: Type of
    any: (A: Type) -> A -> Any

use Any.any

foo: List Bool =
    prepend Bool false (prepend Bool true (empty Bool))

bar: List Any =
    prepend Any (any Unit unit) (prepend Any (any Bool false) (empty Any))
