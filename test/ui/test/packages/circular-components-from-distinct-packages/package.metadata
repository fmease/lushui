# TEST fail check
# @Bug we used to have two highlights before the rewrite, now we only have one
# @Task the dependency "record key" of "both" (ideally all) circular components should be shown

name: left,
version: "0.0.0",
components: {
    left: {
        type: library,
        path: "content.lushui",
        dependencies: { right: { path: right } }
    },
},
