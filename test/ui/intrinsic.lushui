;;; TEST pass run

use extern.core.(
    type.Type,
    nat.(self, Nat),
    bool.Bool,
    list.List,
    tuple.Tuple,
    )

combine: Nat -> Nat -> Nat = nat.+
successor: Nat -> Nat = combine 1

p: Bool = nat.== 50 (nat.add 10 40)
q: Bool = nat.== 1 0

main: Tuple List.((Type), Nat, Nat) = Tuple.(
    (Nat),
    nat.+ 3 6,
    (Nat),
    successor 338389220489284293,
    )
