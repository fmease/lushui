# TEST pass run

use extern.core.nat.(Nat, +, *)

main: Nat =
    let alpha: Nat = 12 in
    let beta: Nat = 100233 in
    let gamma: Nat =
        * alpha (+ beta 1) in
    let gamma: Nat =
        * alpha (+ beta 1)
    in
    let gamma: Nat = + gamma alpha in
    * gamma (* gamma alpha)
