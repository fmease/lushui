module core

use core.nat.Nat
use core.nat.multiply
use core.text.Text
use core.text.concat

;; use core.(nat.(Nat multiply) text.(Text concat))

some-result: Nat = multiply 16 4

do-something (x: Nat): Nat = multiply x 2

nat-identity (x: Nat): Nat = x

alpha: Nat = do-something 3000
beta: Nat = nat-identity 23

;; foo: multiply 2 4 = Nat

name: Text = "Jane"

welcome: Text = concat "welcome, " name
