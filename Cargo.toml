cargo-features = ["profile-rustflags"]

[workspace]
members = [
    "compiler/*",
    "project/library/*",
    "project/tool/*",
    "library/core/source/boot",
]
default-members = ["compiler/main"]
resolver = "2"
