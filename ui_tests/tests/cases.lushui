;;; TEST ignore (deapplications not supported yet)

use crates.core.(
    list.(List cons empty)
    bool.(Bool false true))

is-empty (A: Type) (list: List A): Bool =
    case list of
        empty \A => true
        cons \A \a => false
